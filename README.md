# StarViewDataIntranet
This is the web application for intranet of SVD, and is supposed to be working on Tomcat7.

I lost all java codes because of eclipse misused. So this might include some wrong staff in the codes. I recompile class files into java files and fixed some error part. but it is working well now.
